import React from 'react'

export default function page() {
  return (
    <div>
      This is the docs page from the optional catch all segment
      if you see this, the optional catch all segment is working
    
      if you see this, that means there is no slug in the URL
      <h1>Docs</h1>
    </div>
  )
}
